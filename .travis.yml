language: ruby
rvm:
- 2.3.3

script: jekyll build && htmlproofer ./_site

branches:
  only:
  - master
  - gh-pages
  - /pages-(.*)/

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

sudo: false
